BACKEND_PORT=4000
JWT_SECRET=change_me_to_a_secure_secret
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=movie_dump
DB_SSL=false

# Clave v3 (32 caracteres) — se usa como ?api_key=
TMDB_API_KEY=your_tmdb_v3_api_key

# Read Access Token (JWT eyJ...) — se usa como Authorization: Bearer
# Obtener en: https://www.themoviedb.org/settings/api
TMDB_ACCESS_TOKEN=
